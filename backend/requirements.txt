# ComplAI MVP1 — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Python 3.9+ compatible.
# LangChain ecosystem packages (langgraph, langsmith, langchain-core) are
# intentionally left unpinned to let pip resolve compatible versions together.
# The other packages are pinned for reproducibility.

# ── Web framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # multipart/form-data uploads

# ── Database ───────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9           # PostgreSQL driver

# ── Auth ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0 # JWT
passlib[bcrypt]==1.7.4           # password hashing

# ── Async task queue ───────────────────────────────────────
celery==5.3.6
redis==5.0.4

# ── LangGraph / LangSmith ──────────────────────────────────
# These three must resolve together — do NOT pin exact versions.
# Tested range: langgraph ≥0.1, langchain-core ≥0.2, langsmith ≥0.1.66
langgraph>=0.1.5,<0.3.0
langchain-core>=0.2.10,<0.4.0
langsmith>=0.1.66,<0.3.0

# ── AI / LLM ───────────────────────────────────────────────
anthropic>=0.28.0,<1.0.0

# ── OCR — Google Document AI ───────────────────────────────
google-cloud-documentai==2.24.0

# ── PDF extraction (free, digital PDFs) ────────────────────
pdfplumber==0.11.0
pypdfium2>=4.0.0,<6.0.0      # PDF page → image render (for scanned PDF OCR)

# ── Image handling ─────────────────────────────────────────
Pillow==10.3.0
pillow-heif==0.16.0              # HEIC/HEIF support

# ── Data processing ────────────────────────────────────────
pandas==2.2.2
openpyxl==3.1.2                  # Excel read/write (.xlsx)
xlrd==2.0.1                      # Excel read (.xls legacy format)
xlwt==1.3.0                      # Excel write (.xls legacy format, optional)

# ── Fuzzy matching (party names in bank recon) ─────────────
fuzzywuzzy==0.18.0
python-Levenshtein==0.25.1       # speeds up fuzzywuzzy

# ── Utilities ──────────────────────────────────────────────
typing_extensions>=4.5.0          # Python 3.9 TypedDict backport
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
httpx==0.27.0                    # async HTTP
aiofiles==23.2.1                 # async file I/O

# ── WhatsApp (Twilio) ──────────────────────────────────────
twilio>=8.0.0,<9.0.0            # WhatsApp Business API via Twilio sandbox
